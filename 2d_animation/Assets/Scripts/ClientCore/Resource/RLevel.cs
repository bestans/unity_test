using UnityEngine;
namespace ClientCore {
    /*
    public class RLevel : TSingleton<RLevel> {
        
        private RLevel(){}
        public void LoadLevel(string levelName) {
            RBundleManager.Singleton.LoadLevel(levelName);
        }

        public void LoadLevelAdditive(string levelName) {
            RBundleManager.Singleton.LoadLevelAdditive(levelName);
        }

        public Operation LoadLevelAsync(string levelName) {
            return RBundleManager.Singleton.LoadLevelAsync(levelName);
        }
    } */
}
